# Copy this file to .env and set values as needed.
# Required: path on host for bind mounts — must be writable.
HOST_DATA_PATH=/absolute/path/to/host/data

# Base runtime image that already contains zyra scheduler and /usr/local/bin/rtvideo
# Set to your accessible registry image and login to that registry first.
# Examples (pick one and ensure access):
ZYRA_SCHEDULER_IMAGE=ghcr.io/noaa-gsl/zyra-scheduler:latest

# Credentials used by the CLI (do not commit real values)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
VIMEO_CLIENT_ID=
VIMEO_CLIENT_SECRET=
VIMEO_ACCESS_TOKEN=
OPENAI_API_KEY=
